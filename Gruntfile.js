module.exports = function (grunt) {

	grunt.initConfig({
		distdir: {'dist'},
		copy: {
			test: {
				files: [{
					expand: true,	
					dest: '<%= distdir %>',
					src:
				}],
				src: 
			}
		}
	});
}